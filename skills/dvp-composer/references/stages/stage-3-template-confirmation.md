# 阶段 3：模板确认

## 目标

在缺少模板时，确定 DVP 的格式和结构。

## 流程

1. 检查资料收集中是否已包含 DVP 模板。有则跳过本阶段，直接进入阶段 4。
2. 询问用户是否有公司或项目的 DVP 模板。有则读取并提取章节结构。
3. 用户也没有时，参考内置的 section-catalog，提供一个默认结构供确认。
4. 用户对结构认可（可增删章节、调整顺序、修改列定义）。
5. 将确认后的模板写入 `/tmp/dvp_template.json`，作为后续生成的框架。

## 完成条件

- 已有一份用户认可的 DVP 模板（无论来源）
